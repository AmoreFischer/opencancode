# DEVLOG 示例 — demo-app（虚构项目）

> 演示三条典型条目：普通条目（#1）/ 跨模型接力（#2）/ 跨运行时接力含对账（#3）。最新在最上。所有平台、会话、模型均为占位符。

---

## #3 · 2026-09-12 · 导出功能收尾与回归

**执行元信息**：
- **平台**：<runtime-C>（自治 harness）
- **会话**：`<session-id-c3d4e5>`
- **模型**：`<model-C>`（thinking: medium）
- **模式**：收尾 + 回归验证
- **花费**：订阅内
- **接力点**：**基于 #2**（跨运行时接力；写前已对账：#2 之后基线无其他改动）

**做了什么**
- 补齐 CSV 导出的转义边界用例，全量回归 42/42 通过

**决策**
- 转义统一走单一出口函数，不再散落各调用点（防复发）

**踩坑**
- Windows 下 CRLF 使快照测试抖动 → 归一化后再比对

**产物**
- `export/csv_writer.py`；回归报告 `docs/regression-0912.md`

---

## #2 · 2026-09-11 · CSV 导出功能主体（跨模型接力）

**执行元信息**：
- **平台**：<platform-B>（IDE agent）
- **会话**：`<session-id-4567>`
- **模型**：`<model-B>`（thinking: high）
- **模式**：功能攻坚
- **花费**：按量计费 <额度>
- **接力点**：**基于 #1**（跨模型接力：#1 由 <model-A> 完成）

**做了什么**
- 实现 CSV 导出主体；列配置沿用 #1 定的 schema

**决策**
- 大文件流式写出，不整表入内存

**踩坑**
- 新模型对 #1 的缩写笔记理解偏差 → 教训：接力条目的前条目要写全称，缩写与梗进不了接力合同

**产物**
- `export/` 目录初版

---

## #1 · 2026-09-10 · 项目启动：导出模块立项

**执行元信息**：
- **平台**：<platform-A>（CLI agent）
- **会话**：`<session-id-1234>`
- **模型**：`<model-A>`（thinking: high）
- **模式**：设计 + 骨架
- **花费**：免费层
- **接力点**：新项目启动

**做了什么**
- 定导出 schema（列配置 / 格式开关）；搭模块骨架

**决策**
- schema 单文件维护，导出器只读不写

**踩坑**
- 无

**产物**
- `docs/export-schema.md`；骨架提交 `<commit-ish>`
